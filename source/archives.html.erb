---
title: Archives
subtitle: Blog posts of old
---

<article>
  <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
  <header>
      <h2><%= year %></h2>
      </header>
        <% articles.each do |article| %>
          <p><b><%= article.date.strftime("%B") %> <%= article.date.day.ordinalize %></b>: <a href="<%= article.url %>"><%= article.title %></a><%=  ' - ' + article.data.subtitle unless article.data.subtitle.nil? %></p>
        <% end %>
  <% end %>
  
</article>